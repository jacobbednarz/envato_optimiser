require 'spec_helper'

describe EnvatoOptimiser do
  it 'has a version number' do
    expect(EnvatoOptimiser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
